# Dependencies
node_modules/

# Databricks bundle state (terraform state, sync snapshots, provider binaries)
.databricks/

# Local Claude Code settings (personal permissions)
.claude/settings.local.json

# macOS
.DS_Store
